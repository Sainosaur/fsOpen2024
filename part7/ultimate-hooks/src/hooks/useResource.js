import axios from "axios"

const useResource = (url) => {
    const create = async (data) => {

    }
    const resource = getAll()

    return {
        resource,
        create
    }
}

export default useResource